Add an orange circular background behind the home icon in `src/pages/Index.tsx` while keeping the marker's 24×24 footprint (so positioning and label offsets don't shift).

Replace the current `<img>` marker with a 24×24 `rounded-full` div using `background: #FF6B35` and a subtle shadow, containing the home SVG sized down to 16×16 so it fits with padding inside the circle. The label block (name + score) to the right is untouched.